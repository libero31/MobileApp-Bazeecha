import 'package:bazeecha/utils/colors.dart';
import 'package:bazeecha/widgets/headings.dart';
import 'package:flutter/material.dart';

class MainMinigames extends StatelessWidget {
  const MainMinigames({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: HeaderText(text: "MINIGAMES WILL COME HERE", color: AppColor.red),
    );
  }
}
